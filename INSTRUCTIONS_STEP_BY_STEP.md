# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ: Добавление вкладки "Детализация расчетов"

## 🎯 Цель
Добавить новую вкладку для кураторов с детальной расшифровкой расчетов зарплаты по каждому дню.

---

## ✅ ЭТАП 1: Добавление роли "curator" в Supabase (5 минут)

### Шаг 1.1: Откройте Supabase Dashboard
1. Зайдите на https://supabase.com
2. Откройте ваш проект
3. Перейдите в **SQL Editor**

### Шаг 1.2: Выполните SQL миграцию
1. Откройте файл `sql-migrations/001_add_curator_role.sql`
2. Скопируйте весь код
3. Вставьте в SQL Editor
4. Нажмите **Run**

### Шаг 1.3: Создайте тестового куратора
В SQL Editor выполните:

```sql
INSERT INTO employees (id, fullname, role, password, active)
VALUES ('CUR001', 'Тестовый Куратор', 'curator', 'test123', true)
ON CONFLICT (id) DO UPDATE SET role = 'curator', active = true;
```

### Шаг 1.4: Проверка
```sql
SELECT id, fullname, role FROM employees WHERE role = 'curator';
```

Должна появиться запись с куратором.

---

## ✅ ЭТАП 2: Добавление эндпоинтов в server.cjs (10 минут)

### Шаг 2.1: Откройте server.cjs
Найдите строку примерно 2500 (после всех существующих эндпоинтов, но перед `app.listen`)

### Шаг 2.2: Добавьте middleware для кураторов
ПОСЛЕ строки `const canManageFot = checkRole(['admin']);` добавьте:

```javascript
const canViewDetails = checkRole(['admin', 'accountant', 'curator']);
```

### Шаг 2.3: Добавьте три новых эндпоинта
Скопируйте код из файла `new-endpoints-details.js` и вставьте ПЕРЕД строкой `app.listen`:

```javascript
// Получение списка сотрудников
app.get('/api/get-employees-list', ...

// Получение детализации расчетов
app.post('/api/get-calculation-details', ...

// Экспорт детализации
app.post('/api/export-calculation-details', ...
```

### Шаг 2.4: Сохраните и перезапустите сервер
```bash
# В терминале нажмите Ctrl+C чтобы остановить сервер
# Затем запустите снова:
node server.cjs
```

---

## ✅ ЭТАП 3: Добавление HTML вкладки (5 минут)

### Шаг 3.1: Откройте payroll.html

### Шаг 3.2: Добавьте кнопку в навигацию
Найдите блок `.tab-navigation` (примерно строка 700) и ПОСЛЕ кнопки "Лимиты карты" добавьте:

```html
<button class="tab-button" id="details-tab-button" 
        onclick="switchTab('details', this)" 
        style="display: none;">
    📊 Детализация расчетов
</button>
```

### Шаг 3.3: Добавьте содержимое вкладки
Откройте файл `details-tab-html.html`, скопируйте ВСЁ содержимое и вставьте В КОНЕЦ блока `.content` 
(перед закрывающим `</div>` контейнера), примерно строка 900.

### Шаг 3.4: Сохраните payroll.html

---

## ✅ ЭТАП 4: Добавление JavaScript (5 минут)

### Шаг 4.1: Откройте payroll.js

### Шаг 4.2: Добавьте код для детализации
Откройте файл `details-tab-js.js`, скопируйте ВСЁ содержимое и вставьте В САМЫЙ КОНЕЦ файла payroll.js (после последней функции).

### Шаг 4.3: Добавьте контроль доступа по ролям
В функции `verifyAuthentication()` (примерно строка 40) найдите блок где показываются/скрываются вкладки.

ПОСЛЕ блока с `cardLimitsTabButton` добавьте:

```javascript
// Детализация - для админа, бухгалтера и кураторов
const detailsTabButton = document.getElementById('details-tab-button');
if (detailsTabButton) {
    const canViewDetails = isAdmin || isAccountant || data.user.role === 'curator';
    detailsTabButton.style.display = canViewDetails ? 'block' : 'none';
}

// СПЕЦИАЛЬНО ДЛЯ КУРАТОРОВ: показываем ТОЛЬКО вкладку детализации
if (data.user.role === 'curator') {
    // Скрываем все вкладки кроме детализации
    document.querySelectorAll('.tab-button').forEach(btn => {
        if (btn.id !== 'details-tab-button') {
            btn.style.display = 'none';
        }
    });
    
    // Автоматически открываем вкладку детализации
    setTimeout(() => {
        switchTab('details', detailsTabButton);
    }, 100);
}
```

### Шаг 4.4: Сохраните payroll.js

---

## ✅ ЭТАП 5: Локальное тестирование (10 минут)

### Тест 1: Проверка доступа куратора
1. Откройте http://localhost:3000
2. Войдите под куратором:
   - **Логин:** Тестовый Куратор
   - **Пароль:** test123
3. **Ожидаемый результат:** Должна быть видна ТОЛЬКО вкладка "Детализация расчетов"

### Тест 2: Проверка функционала
1. Выберите сотрудника из списка
2. Выберите месяц и год
3. Нажмите "Показать детализацию"
4. **Ожидаемый результат:** 
   - Таблица с расчетами по дням
   - Итоговая панель с суммами
   - Кнопка экспорта в Excel

### Тест 3: Экспорт
1. Нажмите "Экспорт в Excel"
2. **Ожидаемый результат:** Скачивается файл `.xlsx` с детализацией

### Тест 4: Проверка прав бухгалтера
1. Выйдите (кнопка "Выйти")
2. Войдите под бухгалтером
3. **Ожидаемый результат:** Видны ВСЕ вкладки включая "Детализацию"

### Тест 5: Проверка прав админа
1. Выйдите
2. Войдите под админом
3. **Ожидаемый результат:** Видны ВСЕ вкладки

---

## ✅ ЭТАП 6: Деплой на Fly.io (5 минут)

### Шаг 6.1: Коммит изменений
```bash
cd D:\Shifts-api\Shifts-api
git add .
git commit -m "feat: добавлена вкладка детализации расчетов для кураторов"
```

### Шаг 6.2: Деплой
```bash
fly deploy
```

### Шаг 6.3: Проверка на продакшене
1. Откройте https://shifts-api.fly.dev
2. Войдите под куратором (если создали на prod)
3. Проверьте что всё работает

### Шаг 6.4: Создайте куратора на продакшене
Откройте Supabase Production и выполните:

```sql
INSERT INTO employees (id, fullname, role, password, active)
VALUES ('CUR001', 'Имя Куратора', 'curator', 'пароль', true);
```

---

## 🎉 ГОТОВО!

### Что получилось:
✅ Новая роль "curator" в системе
✅ Вкладка "Детализация расчетов" с полной расшифровкой
✅ Контроль доступа: кураторы видят ТОЛЬКО детализацию
✅ Бухгалтеры и админы видят всё
✅ Экспорт в Excel
✅ Печать отчетов

### Если что-то пошло не так:
1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте логи сервера в терминале
3. Проверьте что все файлы сохранены
4. Перезапустите сервер: `Ctrl+C` затем `node server.cjs`

---

## 📞 Дальнейшие улучшения (опционально):

1. **Фильтры по магазинам** - показывать только сотрудников определенного магазина
2. **Сравнение периодов** - сравнить текущий месяц с прошлым
3. **Графики** - визуализация динамики зарплаты
4. **Push-уведомления** - когда расчет готов
5. **Мобильная версия** - адаптивная верстка для телефонов

Хотите добавить что-то из этого?
